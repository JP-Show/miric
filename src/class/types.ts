// interface categories {
//   categ: 'olá'
// }

// const salud: categories = {
//   name
// }
// // 'watching/reading' | 'plan to watch/read' | 'completed' | 'drop'

// function de (user:string = 'matheus' || ){}

type categorie = Array<'a' | 'b'>

const a: categorie = ['c']
if (a !== a) {
}

type cat = {
  name: 'gatuno'
}

const a: cat = {
  name: ""
}
